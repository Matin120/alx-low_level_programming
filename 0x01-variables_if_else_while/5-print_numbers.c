#include <stdio.h>

/**
* main - program will print all single digit of base 10
* Return: 0 (success)
*/

int main(void)
{
	int digits;

	for (digits = 0; digits <= 9; digits++)
		printf("%d\n", digits);

	return (0);
}
